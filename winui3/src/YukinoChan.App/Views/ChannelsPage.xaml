<?xml version="1.0" encoding="utf-8"?>
<!-- 会话通道 · 管理 / 预检页（计划书 §8.3 / D4 —— 原 RdpPage 改造而成）。

     职能分家：
       本页   = 通道配置（增删改）+ 凭据 + 部署代理 + 环境预检 + 全局执行/通知设置；
       通道页 = 该通道的画面、进度、心跳、事件（左侧菜单里每条通道一项）。
     所以内嵌画面卡在这里是【折叠保留】的 —— 画面归通道页，本页只在自检模式下自己挂一下。 -->
<Page
    x:Class="YukinoChan.Views.ChannelsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:YukinoChan.Models"
    xmlns:views="using:YukinoChan.Views">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="16,12" Spacing="12">

            <!-- 标题与总开关 -->
            <StackPanel Spacing="4">
                <TextBlock Text="会话通道" Style="{StaticResource SubtitleTextBlockStyle}" />
                <TextBlock TextWrapping="Wrap" Opacity="0.7"
                           Text="一条通道 = 一个目标账户 + 它自己的凭据和指令桥目录。任务在「任务执行」页各自选走哪条通道；开始执行后通道之间并行，通道内部仍按顺序跑。没选通道的任务在主控端当前会话里执行（本地执行）。" />
            </StackPanel>

            <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    Padding="14">
                <StackPanel Spacing="10">
                    <ToggleSwitch Header="在目标账户会话中执行任务"
                                  OnContent="已启用（点「开始执行」时会先连接各通道的目标账户）"
                                  OffContent="关闭（全部任务都在当前账户本地执行）"
                                  IsOn="{x:Bind VM.RdpSettings.Enabled, Mode=TwoWay}" />

                    <TextBlock TextWrapping="Wrap" Opacity="0.65" FontSize="12"
                               Text="多条通道能否同时跑，取决于系统是否允许多个交互式会话并存：系统允许多会话时各通道互不影响；普通 Windows 客户端版同一台机器只允许一个交互式会话，第二条通道会顶掉前一个桌面，这种情况请把通道分到不同机器。" />
                </StackPanel>
            </Border>

            <!-- ===== 通道列表 ===== -->
            <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    Padding="14">
                <StackPanel Spacing="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="通道" FontWeight="SemiBold" VerticalAlignment="Center" />

                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                            <Button Content="新建通道" Click="OnAddChannel"
                                    ToolTipService.ToolTip="加一条通道：填目标主机与账户，然后保存凭据" />
                            <Button Content="复制" Click="OnDuplicateChannel"
                                    ToolTipService.ToolTip="按当前选中的通道复制一条（凭据需要重新保存）" />
                            <Button Content="删除" Click="OnRemoveChannel" />
                            <Button Content="重新预检" Click="OnRefreshChannelReadiness" />
                        </StackPanel>
                    </Grid>

                    <TextBlock TextWrapping="Wrap" Opacity="0.65" FontSize="12"
                               Text="同一台机器上的多条通道请配不同账户 —— 同一个账户开两条通道没有意义（它们会抢同一个会话）。" />

                    <Border Height="200"
                            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="6">
                        <ListView x:Name="ChannelList"
                                  ItemsSource="{x:Bind VM.Channels, Mode=OneWay}"
                                  SelectedItem="{x:Bind VM.SelectedChannel, Mode=TwoWay}"
                                  SelectionMode="Single">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:RdpChannel">
                                    <StackPanel Padding="4,6" Spacing="2">
                                        <TextBlock Text="{x:Bind Name, Mode=OneWay}" />
                                        <TextBlock Text="{x:Bind ListSubtitle, Mode=OneWay}"
                                                   Opacity="0.65" FontSize="12"
                                                   TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>

                    <InfoBar x:Name="ChannelEmptyBar"
                             IsOpen="False"
                             IsClosable="False"
                             Severity="Informational"
                             Title="还没有配置通道"
                             Message="点「新建通道」加一条，填上目标主机与账户，再保存凭据。只想在本机当前账户跑任务的话，不配通道也可以 —— 那属于「本地执行」。" />
                </StackPanel>
            </Border>

            <!-- ===== 通道编辑 ===== -->
            <Border x:Name="ChannelEditorCard"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    Padding="14">
                <StackPanel Spacing="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="通道设置" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" x:Name="ChannelEditorHint"
                                   Opacity="0.6" FontSize="12" VerticalAlignment="Center" />
                    </Grid>

                    <TextBox Header="通道名称"
                             PlaceholderText="例如 1 号机 · Player2（留空会按账户名自动起一个）"
                             Text="{x:Bind VM.SelectedChannel.Name, Mode=TwoWay}"
                             LostFocus="OnChannelFieldLostFocus" />

                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                 Header="目标主机"
                                 PlaceholderText="127.0.0.1（可填 IP、主机名，或带端口如 192.168.1.20:3390）"
                                 Text="{x:Bind VM.SelectedChannel.Host, Mode=TwoWay}"
                                 LostFocus="OnChannelFieldLostFocus" />
                        <Button Grid.Column="1" Content="用本机" VerticalAlignment="Bottom"
                                Click="OnUseLocalHost"
                                ToolTipService.ToolTip="填回 127.0.0.1，切本机另一个账户" />
                    </Grid>

                    <TextBox Header="目标账户"
                             PlaceholderText="例如 Player2 或 机器名\Player2"
                             Text="{x:Bind VM.SelectedChannel.User, Mode=TwoWay}"
                             LostFocus="OnChannelFieldLostFocus" />

                    <TextBox Header="指令桥目录（仅远程主机需要）"
                             PlaceholderText="留空＝本机 ProgramData 下的默认位置（按账户名自动分目录）"
                             Text="{x:Bind VM.SelectedChannel.BridgePath, Mode=TwoWay}"
                             LostFocus="OnChannelFieldLostFocus" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="实际桥目录：" Opacity="0.7" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" Text="{x:Bind VM.ChannelBridgeDir, Mode=OneWay}"
                                   TextTrimming="CharacterEllipsis" FontSize="12" Opacity="0.8"
                                   VerticalAlignment="Center" Margin="6,0" />
                        <Button Grid.Column="2" Content="打开" Click="OnOpenChannelBridgeFolder" />
                    </Grid>

                    <TextBlock TextWrapping="Wrap" Opacity="0.65" FontSize="12"
                               Text="桥目录是主控端与目标会话里的代理互相传指令/状态的地方，留空时按「账户名」自动分目录（每条通道一个），所以本机多账户不用手动区分。目标是别的机器时，对方读不到本机文件，必须填一个双方都能读写的共享位置。" />

                    <PasswordBox x:Name="ChannelPasswordInput"
                                 Header="登录密码"
                                 PlaceholderText="仅用于存入 Windows 凭据管理器"
                                 PasswordChar="●" />

                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="保存凭据" Click="OnSaveChannelCredential" />
                        <Button Content="清除已保存凭据" Click="OnClearChannelCredential" />
                        <TextBlock VerticalAlignment="Center" Opacity="0.7" FontSize="12">
                            <Run Text="凭据：" />
                            <Run Text="{x:Bind VM.ChannelCredentialText, Mode=OneWay}" />
                        </TextBlock>
                    </StackPanel>

                    <TextBlock TextWrapping="Wrap" Opacity="0.65" FontSize="12"
                               Text="密码只进 Windows 凭据管理器（按「主机 + 账户」各存一条，通道之间互不覆盖），不会写进 config.json。目标账户必须设置过登录密码。" />

                    <ComboBox Header="远程桌面窗口尺寸"
                              ItemsSource="{x:Bind VM.ResolutionItems}"
                              DisplayMemberPath="Value"
                              SelectedValuePath="Key"
                              SelectedValue="{x:Bind VM.ChannelResolutionKey, Mode=TwoWay}"
                              HorizontalAlignment="Stretch" />

                    <ComboBox Header="该通道任务全部结束后的会话处理"
                              ItemsSource="{x:Bind VM.SessionFinishItems}"
                              DisplayMemberPath="Value"
                              SelectedValuePath="Key"
                              SelectedValue="{x:Bind VM.SelectedChannel.SessionFinish, Mode=TwoWay}"
                              SelectionChanged="OnChannelFinishChanged"
                              HorizontalAlignment="Stretch" />

                    <ToggleSwitch Header="启用该通道"
                                  OnContent="启用（参与并行执行）"
                                  OffContent="停用（该通道的任务不会下发）"
                                  IsOn="{x:Bind VM.SelectedChannel.Enabled, Mode=TwoWay}"
                                  Toggled="OnChannelEnabledToggled" />

                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="保存通道设置" Click="OnSaveChannelEdits"
                                Style="{StaticResource AccentButtonStyle}" />
                    </StackPanel>

                    <TextBlock TextWrapping="Wrap" Opacity="0.65" FontSize="12"
                               Text="文本框失焦时会自动落盘，按钮只是让你能确认一次。选「注销用户」收尾要注意：注销会销毁目标会话，里面正在跑的代理与脚本会被系统一并终止。" />
                </StackPanel>
            </Border>

            <!-- ===== 该通道预检 ===== -->
            <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    Padding="14">
                <StackPanel Spacing="10">
                    <TextBlock Text="通道预检" FontWeight="SemiBold" />

                    <InfoBar x:Name="ChannelReadinessBar"
                             IsOpen="True"
                             IsClosable="False"
                             Severity="Informational"
                             Title="检查结果"
                             Message="{x:Bind VM.ChannelReadinessText, Mode=OneWay}" />

                    <!-- 同主机多通道提示（D5：只提示不禁用）。
                         Windows 客户端版同时只允许一个交互式会话，同一台机器上的两条通道会互相接管桌面。 -->
                    <InfoBar IsOpen="{x:Bind VM.HasSameHostHint, Mode=OneWay}"
                             IsClosable="False"
                             Severity="Warning"
                             Title="同一主机有多条通道"
                             Message="{x:Bind VM.ChannelSameHostHint, Mode=OneWay}" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="目标会话：" Opacity="0.7" />
                        <TextBlock Grid.Column="1" Text="{x:Bind VM.ChannelSessionSummary, Mode=OneWay}" TextWrapping="Wrap" />
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="代理状态：" Opacity="0.7" />
                        <TextBlock Grid.Column="1" Text="{x:Bind VM.ChannelAgentStatusText, Mode=OneWay}" TextWrapping="Wrap" />
                    </Grid>

                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="开启远程桌面" Click="OnEnableRemoteDesktop"
                                ToolTipService.ToolTip="需要管理员权限：写入注册表并放行防火墙（对整机生效，与具体通道无关）" />
                        <Button Content="部署会话代理" Click="OnDeployChannelAgent"
                                ToolTipService.ToolTip="把代理放进所有用户共享的启动目录，目标账户登录后自动接管任务（整机共用一份）" />
                        <Button Content="移除代理" Click="OnRemoveAgent" />
                        <Button Content="重新检查" Click="OnRefreshChannelReadiness" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- ===== 全局执行与通知设置 ===== -->
            <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    Padding="14">
                <StackPanel Spacing="10">
                    <TextBlock Text="执行与通知（全局）" FontWeight="SemiBold" />

                    <ComboBox Header="连接方式"
                              ItemsSource="{x:Bind VM.ClientModeItems}"
                              DisplayMemberPath="Value"
                              SelectedValuePath="Key"
                              SelectedValue="{x:Bind VM.RdpSettings.ClientMode, Mode=TwoWay}"
                              HorizontalAlignment="Stretch" />

                    <CheckBox Content="内嵌模式下在本机播放远端声音"
                              IsChecked="{x:Bind VM.RdpSettings.AudioEnabled, Mode=TwoWay}" />

                    <CheckBox Content="GFX 全帧管线（实验性：撕裂根治，需重连生效）"
                              IsChecked="{x:Bind VM.RdpSettings.GfxEnabled, Mode=TwoWay}"
                              ToolTipService.ToolTip="走 RDP 图形管道协商 progressive 全帧，远端快速变化时不再撕裂；默认关闭" />

                    <TextBlock TextWrapping="Wrap" Opacity="0.65" FontSize="12"
                               Text="选「内嵌」时画面显示在各会话通道页面里，鼠标键盘可直接操作；选「独立窗口」时用系统 mstsc 开窗口（稳定，永久可用）。改连接方式后下次连接生效。" />

                    <NumberBox Header="等待目标会话建立的超时（秒）"
                               Minimum="15"
                               Maximum="600"
                               SpinButtonPlacementMode="Compact"
                               Value="{x:Bind VM.RdpSettings.ConnectTimeoutSeconds, Mode=TwoWay}" />

                    <ToggleSwitch Header="每完成一个任务都发系统通知"
                                  IsOn="{x:Bind VM.RdpSettings.NotifyOnTaskDone, Mode=TwoWay}" />
                    <ToggleSwitch Header="全部任务完成后发汇总通知"
                                  IsOn="{x:Bind VM.RdpSettings.NotifyOnAllDone, Mode=TwoWay}" />
                    <ToggleSwitch Header="本地（非远程会话）执行时也发通知"
                                  IsOn="{x:Bind VM.RdpSettings.NotifyLocalTaskDone, Mode=TwoWay}" />

                    <TextBlock TextWrapping="Wrap" Opacity="0.65" FontSize="12"
                               Text="系统通知只在发出它的那个用户会话里显示，所以远程任务完成时，目标会话和主控端会各发一次，你在哪边都看得到。" />

                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="连接并执行（当前设置那一组）"
                                Command="{x:Bind VM.ConnectAndRunCommand}"
                                ToolTipService.ToolTip="不按通道分组，直接用上面这块的全局设置连一次并跑全部启用任务" />
                        <Button Content="断开目标会话" Command="{x:Bind VM.DisconnectSessionCommand}" />
                        <Button Content="注销目标账户" Command="{x:Bind VM.LogoffSessionCommand}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- ===== 本轮运行状态（全部通道的镜像） ===== -->
            <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    Padding="14">
                <StackPanel Spacing="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="本轮运行状态" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" x:Name="CurrentChannelHint"
                                   Opacity="0.6" FontSize="12" VerticalAlignment="Center" />
                    </Grid>

                    <!-- 代理失联告警：会话断开/注销后代理会被连带终止，必须显式提示，
                         否则界面永远停在「运行中」，分不清是卡住还是已经死了 -->
                    <InfoBar IsOpen="{x:Bind VM.RdpIsStale, Mode=OneWay}"
                             IsClosable="False"
                             Severity="Error"
                             Title="会话代理已失联"
                             Message="已经超过 20 秒没收到心跳，目标会话可能已断开或被注销 —— 会话里的代理与脚本进程会随之终止。若你是主动断开远程桌面，请用「断开连接」而不是「注销目标账户」，任务才能继续在后台跑。" />

                    <ProgressBar Minimum="0"
                                 Maximum="{x:Bind VM.RdpTotal, Mode=OneWay}"
                                 Value="{x:Bind VM.RdpProgress, Mode=OneWay}"
                                 ShowPaused="{x:Bind VM.IsRdpBusy, Mode=OneWay}" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="状态：" Opacity="0.7" />
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{x:Bind VM.RdpStatusText, Mode=OneWay}" TextWrapping="Wrap" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="当前任务：" Opacity="0.7" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{x:Bind VM.RdpCurrentTask, Mode=OneWay}" TextWrapping="Wrap" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="进度：" Opacity="0.7" />
                        <TextBlock Grid.Row="2" Grid.Column="1">
                            <Run Text="{x:Bind VM.RdpProgress, Mode=OneWay}" />
                            <Run Text=" / " />
                            <Run Text="{x:Bind VM.RdpTotal, Mode=OneWay}" />
                        </TextBlock>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="当前任务已运行：" Opacity="0.7" />
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{x:Bind VM.RdpElapsedText, Mode=OneWay}" />

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="最后心跳：" Opacity="0.7" />
                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{x:Bind VM.RdpHeartbeatText, Mode=OneWay}" />
                    </Grid>

                    <TextBlock TextWrapping="Wrap" Opacity="0.65" FontSize="12"
                               Text="这里显示的是各通道里最靠前的那条 —— 每条通道自己的进度、心跳与事件请点左侧菜单里对应的通道页看。" />

                    <TextBlock Text="远程事件" FontWeight="SemiBold" />

                    <Border Height="160"
                            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="6">
                        <ListView ItemsSource="{x:Bind VM.RdpEvents, Mode=OneWay}" SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:RdpTaskEvent">
                                    <Grid Padding="10,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="70" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{x:Bind Time}" Opacity="0.6" FontSize="12" />
                                        <TextBlock Grid.Column="1"
                                                   Text="{x:Bind IsAbnormal}"
                                                   FontSize="12"
                                                   Opacity="0.7"
                                                   Margin="0,0,8,0" />
                                        <TextBlock Grid.Column="2" Text="{x:Bind Headline}" FontSize="12" TextTrimming="CharacterEllipsis" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                </StackPanel>
            </Border>

            <!-- 画面已搬到通道页（计划书 §8.2 / D3）—— 本页只做配置与预检。 -->
            <Border Padding="14" Style="{StaticResource YukinoCardStyle}">
                <StackPanel Spacing="8">
                    <TextBlock Text="画面在「会话通道」页面里" FontWeight="SemiBold" />
                    <TextBlock TextWrapping="Wrap" Opacity="0.75" FontSize="12"
                               Text="开始执行后，左侧菜单会在本项下面列出本轮每条会话通道；点进去就能看到该通道自己的画面、进度、心跳与事件。" />
                    <Button Content="打开当前通道画面" Click="OnOpenActiveChannelPage" />
                </StackPanel>
            </Border>

            <!-- 内嵌画面宿主：正常模式下折叠（画面归通道页），只有自检模式（embed-vm 参数）才自己挂上来。
                 输入转发 / 全屏 / 黑白屏自愈那套代码保留在这里，M6 多画面时统一收口。 -->
            <Border x:Name="EmbedCard"
                    Visibility="Collapsed"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    Padding="14">
                <StackPanel Spacing="10">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock x:Name="EmbedCardTitle" Text="内嵌画面（实验性）" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <TextBlock Text="连接方式选「内嵌」时，画面显示在各会话通道页面里；这里只在自检模式下使用。"
                                   Opacity="0.6" FontSize="12" VerticalAlignment="Center" TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button x:Name="EmbedConnectButton" Content="连接预览" Click="OnEmbedConnect" Style="{StaticResource AccentButtonStyle}" />
                        <Button x:Name="EmbedDisconnectButton" Content="断开" Click="OnEmbedDisconnect" IsEnabled="False" />
                        <Button x:Name="EmbedFullScreenButton" Content="全屏" Click="OnEmbedFullScreenToggle" IsEnabled="False" />
                        <CheckBox x:Name="EmbedAudioCheck" Content="播放远端声音" IsChecked="True" VerticalAlignment="Center" />
                        <TextBlock x:Name="EmbedStatusText" VerticalAlignment="Center" Opacity="0.7" FontSize="12" />
                    </StackPanel>
                    <TextBlock FontSize="12" Opacity="0.6" TextWrapping="Wrap"
                               Text="输入提示：点击画面获得焦点后，鼠标与键盘直接作用于远端。全屏时 Win 键、Alt+Tab 等系统组合键全部转投远端（F11 退出全屏）；Ctrl+Alt+Del 是系统安全序列任何客户端都无法转发，远端任务管理器请用 Ctrl+Shift+Esc。" />
                    <Border x:Name="EmbedHostBorder" Height="360">
                        <views:RdpView x:Name="EmbedView" />
                    </Border>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
